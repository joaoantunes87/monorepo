export function sum(one: number, two: number): number {
  const result = one + two;
  return result;
}

export function sub(one: number, two: number): number {
  return one - two;
}

export function times(one: number, two: number): number {
  return one * two;
}
